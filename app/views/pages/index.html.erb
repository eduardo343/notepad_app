<div class="container mt-4">
<h2>Pagesin "<%= @notebook.name %>"</h2>

<ul class="list-group">
<%= @pages.each do |page| %>
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <%= link_to "#{page.emoji}#{page.title}", [@notebook, page] %>
    <span class="text-muted"><%= time_ago_in_words(page.created_at) %>Back</span>
  </li>
  <% end %>
</ul>
<%= link_to "➕ New page", new_notebook_page_path(@notebook), class: "btn btn-primary mt-3" %>
</div>